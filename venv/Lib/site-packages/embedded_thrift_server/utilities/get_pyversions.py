"""
Script that finds the newest python version available on linux machine, capped at python 3.7
Author: Evan Canter
"""
import os, re
max = -30
ver = '3.0'
versions = []
for item in os.scandir('/usr/bin/'):
    version = re.search('python(3\.(\d))$', item.name)
    if version is not None and int(version.group(2)) > 6:
        ver = version.group(1)
        versions.append(ver)
print(min(versions, key=float))
