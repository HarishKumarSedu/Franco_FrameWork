from cl_test_station.host_controllers.sim_host_controller.sim_host_controller import SimHostController


class SimThriftController(SimHostController):
    """Simulation Thrift Controller class"""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

    def format_hostname(self, *args, **kwargs):
        """Simulated function"""
        self.log.debug(f"SimThriftController.format_hostname(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def get_hostname(self, *args, **kwargs) -> str:
        """Simulated function. Returns empty string."""
        return ''

    def get_py_version(self, *args, **kwargs) -> str:
        """Simulated function. Returns empty string."""
        return ''

    def ssh_sendcommand(self, *args, **kwargs):
        """Simulated function. Does not actually send an ssh command."""
        self.log.debug(f"SimThriftController.ssh_sendcommand(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def ssh_communicate(self, *args, **kwargs):
        """Simulated function. Does not actually send an ssh command."""
        return ''

    def has_open_ssh(self, *args, **kwargs):
        """Simulated function. Returns True."""
        return True

    def ping(self, *args, **kwargs):
        """Simulated function. Returns 'Received'."""
        return 'Received'

    def ping_test_station(self, *args, **kwargs):
        """Simulated function. Returns 'success'."""
        return 'success'

    def reset_dut(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.reset_dut(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def reset_wrapper(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.reset_wrapper(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def set_i2c_bus_speed(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.set_i2c_bus_speed(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def mmap_add(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.mmap_add(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def set_gpio(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.set_gpio(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def toggle_gpio(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.toggle_gpio(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def get_image_version(self, *args, **kwargs):
        """Simulated function. returns '1.0.0'."""
        return '1.0.0'

    def download(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.download(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def reboot(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.reboot(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def update(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.update(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def restart_thrift_server(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.restart_thrift_server(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def embed_test_station(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console. Returns 0."""
        self.log.debug(f"SimThriftController.embed_test_station(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")
        return 0

    def burst(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.burst(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")

    def server_eval(self, *args, **kwargs):
        """Simulated function. Prints attempted call to the console."""
        self.log.debug(f"SimThriftController.server_eval(), args: {args}, kwargs: ({', '.join(kwargs.keys())})")
