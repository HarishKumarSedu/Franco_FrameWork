import os
def get_pkg_rel_path(pkg):
    base_name = pkg.__name__.split('.')[0]
    norm_path = os.path.normpath(pkg.__file__)
    path_list = norm_path.split(os.sep)
    rel_path = os.sep.join(path_list[path_list.index(base_name):])
    return rel_path
