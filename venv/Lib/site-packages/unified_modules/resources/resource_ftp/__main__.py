"""
Command line support for MyFtpServer.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_ftp --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_ftp import MyFtpServer


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(MyFtpServer)
