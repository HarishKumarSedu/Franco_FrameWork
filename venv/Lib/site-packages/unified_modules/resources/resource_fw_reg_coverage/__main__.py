"""
Command line support for FwRegCoverage.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.fw_reg_coverage --interactive

Type ``quit`` and hit <enter> to terminate.

"""
from unified_modules.test_support.cli_support import CliOptions

from .fw_reg_coverage import FwRegCoverage


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(FwRegCoverage)