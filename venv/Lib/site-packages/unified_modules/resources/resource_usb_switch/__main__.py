"""
Command line support for ResourceUsbSwitch.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_usb_switch --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_usb_switch import ResourceUsbSwitch


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceUsbSwitch)
