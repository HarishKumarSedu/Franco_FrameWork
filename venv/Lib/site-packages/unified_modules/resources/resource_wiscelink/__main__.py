"""
Command line support for ResourceWisceLink.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_wav_generator --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_wiscelink import ResourceWisceLink


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceWisceLink)
