"""
Command line support for Cli.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_cli --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_cli import ResourceCli


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceCli)
