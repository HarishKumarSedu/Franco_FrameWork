"""
Command line support for ResourceCLIVE.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_fpga --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_fpga import ResourceCLIVE


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceCLIVE)
