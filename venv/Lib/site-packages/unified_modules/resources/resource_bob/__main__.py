"""
Command line support for ResourceBOB.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_bob --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_bob import ResourceBOB


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceBOB)
