"""
Command line support for

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_data_lib --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_data_lib import ResourceDataLib


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceDataLib)
