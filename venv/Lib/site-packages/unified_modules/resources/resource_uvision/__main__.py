"""
Command line support for ResourceUvision.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_uvision --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_uvision import ResourceUvision


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceUvision)
