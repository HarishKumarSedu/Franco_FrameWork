"""
Command line support for Playback and Record.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_pa_multitrack --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_audio_playrec_pamultitrack import ResourceRecordPlayAudio


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceRecordPlayAudio)
