"""
Command line support for math library.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_math_lib --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_math_lib import ResourceMathLib


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceMathLib)
