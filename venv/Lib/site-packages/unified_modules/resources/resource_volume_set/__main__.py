"""
Command line support for Setting system volume.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_volume_set --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_volume_set import ResourceVolumeSet


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceVolumeSet)
