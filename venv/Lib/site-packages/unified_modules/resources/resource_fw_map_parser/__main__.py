"""
Command line support for Firmware map parser.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_firmware_map_parser --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_firmware_map_parser import ResourceFirmwareMapParser


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceFirmwareMapParser)
