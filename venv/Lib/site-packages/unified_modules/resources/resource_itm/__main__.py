"""
Command line support for ITM library.

Usage:

.. code-block:: sh

    $ python -m unified_modules.resources.resource_itm_lib --interactive

Type ``quit`` and hit <enter> to terminate.

"""

from unified_modules.test_support.cli_support import CliOptions

from .resource_itm_lib import ResourceItmLib


if __name__ == "__main__":
    # Start resource as an interactive command line service:
    RUN = CliOptions(ResourceItmLib)
